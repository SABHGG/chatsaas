---
version: 1
slug: "apps-web-src-app-board-page-tsx"
primary_target: "apps/web/src/app/board/page.tsx"
related_targets: []
---

Scope and visitor mode: the /board surface (list + header + selected-line detail pane), Operate mode.
Audience, job, action: non-technical small-business owners scanning their chatbot lines; job = know where every line stands and act on it; primary actions = plug in a new line, read state, unplug, copy embed.
Chosen direction: THE INDEX RAIL (surface-scope roll b015bf69, reroll 2, key board-reroll2, locked 2026-08-31). The sidebar is the index of lines — name plus state lamp, selectable — and the selected line's full detail (status, usage, embed when live, plug/unplug) stays open beside it. Zero navigation: the old detail route becomes a redirect into the pane. Memorable moment: the lamp-lit index card of the line that just went live.
Constraints: committed world unchanged (Live Line / Jack Label / Flat-by-Default, amber ration, ivory/ink); the 4-step wizard is out of scope and untouched; existing token layer only.
Unresolved decisions: none material.
