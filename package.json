{
  "name": "@chatsaas/root",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "dev:api": "pnpm --filter @chatsaas/functions dev:sandbox",
    "dev:ingest": "pnpm --filter @chatsaas/functions exec tsx scripts/sandbox-ingest-bot.mjs",
    "dev:ingest-doc": "pnpm --filter @chatsaas/functions exec tsx scripts/sandbox-ingest.mjs",
    "dev:sandbox": "pnpm --filter @chatsaas/web exec sh -c 'set -a; . ./.env.local; set +a; next dev -p 3000'",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "typescript": "^5.4.5"
  }
}
